name: Hello World

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Print hello
              run: echo "Hello World!"

            - name: Print Goodbye
              run: echo "Goodbye World!"
